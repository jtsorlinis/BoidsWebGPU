{
  "name": "boidswebgpu",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build --base=/BoidsWebGPU/ && gh-pages -d dist"
  },
  "devDependencies": {
    "@babylonjs/core": "^7.3.2",
    "gh-pages": "^6.1.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.10"
  }
}
